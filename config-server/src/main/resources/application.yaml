spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///home/rrojase/projects/pocs/microservices-ecommerce/config-data
          clone-on-start: true
          default-label: main

server:
  port: 8888